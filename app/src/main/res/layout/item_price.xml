<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="46dp"
    android:orientation="horizontal" android:baselineAligned="false"
    android:gravity="center_vertical">

    <TextView android:id="@+id/tvName"
        android:layout_width="0dp" android:layout_weight="2.4"
        android:layout_height="wrap_content"
        android:textSize="12sp" android:textStyle="bold" android:textColor="@color/txt"/>

    <EditText android:id="@+id/etKg"
        android:layout_width="0dp" android:layout_weight="1.5"
        android:layout_height="38dp" android:background="@drawable/bg_input"
        android:gravity="center" android:inputType="numberDecimal"
        android:textSize="14sp" android:textStyle="bold" android:maxLines="1"
        android:padding="2dp"/>

    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="  kg / 16ft" android:textSize="10sp" android:textColor="@color/mut"/>
</LinearLayout>
